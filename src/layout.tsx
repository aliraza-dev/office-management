import "./globals.css";

export const metadata = {
  title: "OMS",
  description: "Office Management Solution",
};

interface RootLayoutProps {
  children: React.ReactNode;
}

export default function RootLayout({ children }: RootLayoutProps) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
